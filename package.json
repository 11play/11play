{
  "name": "11play-web",
  "version": "1.0.0",
  "private": true,
  "description": "11Play production validation, static review build and Firebase Firestore Security Rules testing.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "check:js": "find js admin reviews scripts -type f -name '*.js' -print0 | xargs -0 -n1 node --check && if [ -d tests ]; then find tests -type f -name '*.js' -print0 | xargs -0 -n1 node --check; fi",
    "build:reviews": "node scripts/build-reviews.js",
    "validate:production": "node scripts/validate-production.js",
    "test:rules": "firebase emulators:exec --only firestore --project demo-11play \"node tests/firestore.rules.test.js\"",
    "test": "npm run test:rules",
    "validate": "npm run check:js && npm run build:reviews && npm run validate:production && npm run test:rules",
    "deploy:firestore": "firebase deploy --only firestore --project web11-one"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}